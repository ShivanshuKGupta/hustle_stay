import 'package:flutter/material.dart';

class HostelComplaint extends StatelessWidget {
  const HostelComplaint({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Your Complaints'),
      ),
      body: Container(),
    );
  }
}
